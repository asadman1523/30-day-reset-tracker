# 30 天重建追蹤 — AGENTS.md

## 核心原則

- 台北時區執行每日紀錄與晚間檢核。
- `30天重建追蹤.xlsx` 是唯一主資料來源（single source of truth）。
- 在詢問、判斷今日缺漏、回覆今日狀態、建議今天的重訓動作／重量或有氧參數之前，必須先讀取工作簿現況；不可只依賴對話記憶、前次紀錄或預設題目。
- 使用者提供任何新的追蹤資料後，工作不算完成，直到資料成功寫回同一本 `30天重建追蹤.xlsx` 並完成驗證。
- 不得只在聊天中回覆「收到／已記錄」卻沒有實際更新工作簿。
- 更新現有工作簿，不建立副本。

## 強制寫回與驗證

每次收到新的飲食、重訓、有氧、睡眠、散步、下注／盤口狀態、衝動分數、觸發原因、替代行動等資料時，必須依序：

1. 讀取工作簿／目前匯出資料。
2. 找到正確日期與工作表。
3. 寫入新資料，保留既有資料、格式與公式。
4. 儲存同一本工作簿。
5. 重新開啟工作簿驗證寫入結果。
6. 檢查日期、餐別、食物、蛋白質、碳水、熱量、動作、組次、重量、次數、DBR、有氧時間、坡度、速度與其他欄位是否寫在正確位置。
7. 檢查訓練量公式與零下注連續天數公式沒有錯誤。
8. 每次重訓寫入後必須重新產生「動作基準」工作表。
9. 只有 `tracker_update_result.json` 對本次每一筆更新皆顯示 `verified: true` 後，才能向使用者表示已完成紀錄。

## 飲食紀錄

- 檢查「飲食紀錄」的當天資料：早餐、午餐、晚餐。
- 餐點內容已有文字，或明確寫「未進食」，即視為該餐完成，不得重問。
- 一餐可能有多筆資料；只要已有餐點內容，就不重問該餐。
- 收到新餐點後，要記錄餐點、來源、蛋白質、碳水、熱量與備註。
- 有產品標示時優先採用標示；沒有時可做透明估算並記錄假設。
- 不自行猜份量、飽足感、醬料或未提及食材。

## 每日檢核

檢查「每日檢核」當天的：零下注、查看盤口、衝動分數、觸發原因、替代行動、完成重訓、散步分鐘、睡眠小時。

- 「是」「否」「0」都是有效答案，不能因為值為零或否而重問。
- 只有當天曾查看盤口，或衝動分數大於 0，才要求「觸發原因」與「替代行動」。
- 若未查看盤口且衝動為 0，觸發原因與替代行動可留白，且視為完整。
- 問「是否下注」時，未下注＝「零下注：是」。
- 「散步分鐘」只記一般慢走／散步，不可把跑步機爬坡、正式有氧或重訓後有氧混進去。

## 重訓

- 若「重訓紀錄」已有當天動作資料，或「每日檢核」的完成重訓已有「是／否」，不得再問是否重訓。
- 每一組獨立保存：動作、組次、重量、次數、DBR、訓練量與備註。
- `DBR` 固定代表「還能做幾下（reps in reserve）」：DBR0＝無法再完成 1 下標準次數，DBR1＝約還能 1 下，以此類推；不得再把新紀錄當成 RPE。
- 未提供 DBR 時留空，不猜測。
- 重量單位預設公斤；啞鈴為單手重量；自重動作重量填 0 並註記「自重」。
- 若使用者同一動作後續組沒有重複重量，沿用上一組重量並在備註標示。
- 若回報疼痛或不適，必須寫入該次動作備註；後續「動作基準」應標示疼痛／不適，不能單純依重量推薦。

## 動作基準（長期重量記憶）

- 工作簿內的「動作基準」是每個重訓動作的長期記憶。任何重訓規劃、動作選擇或「下次用幾公斤」的回答，都必須先讀這張表。
- 初始化與重建時至少掃描 2026-08-01 至今全部重訓紀錄，不能只看最近幾天；很久沒做的動作也必須保留。
- 每次新增／修改任何重訓組後，工作流程必須自動重建「動作基準」。
- 「建議下次重量」不是歷史最大重量，也不是最近碰過的最重重量。優先依最近一次訓練的可重複工作組判斷：次數、DBR、同重量能穩定完成幾組、是否有 DBR0 崩落、以及疼痛／不適。
- 一般工作組目標以 10–15 下、DBR1–3 為主；特殊動作可依歷史表現調整。最後一組可視情況到 DBR0，但不能因為單一力竭組就把重量自動上調。
- 若最近一次同一重量有多組落在合理次數且 DBR1–3，該重量優先視為下次工作重量；若全部已知組都 DBR0，下一次採更保守重量重新建立工作組。
- 若最近一次有疼痛／不適，保留最近使用重量作歷史，但「建議下次重量」留空並標示注意，直到重新確認無痛。
- 「最近訓練摘要」要保留最近一次各組重量／次數／DBR，避免只看到單一最高組。
- 使用者明確更正器材刻度、重量單位或表示某重量不適合時，以最新資訊為準，不得被舊基準覆蓋。

## 有氧紀錄

- 「跑步機爬坡／爬坡走」與一般散步是不同資料來源，必須寫入獨立的「有氧紀錄」工作表，不得只記成「散步分鐘」。
- 每一段有氧至少記：日期、有氧類型、段次、時間（分鐘）、坡度（%）、速度（km/h）。若使用者有提供距離，也記距離（km）。
- 使用者只說「爬坡」但沒有時間、坡度或速度時，不可猜數字；缺什麼就保留空白或詢問。
- 同一天若有不同坡度／速度的區段，用不同「段次」分開保存。
- 未來在比較爬坡進步時，應以「時間＋坡度＋速度」一起看，而不是只比較分鐘數。

## 問話風格與邊界

- 使用繁體中文，直接、簡潔。
- 不安慰、不說教、不提供運彩盤口、勝率、投注、翻本或期貨交易建議。
- 不因使用者想「再做一點」而鼓勵無限制加量；以已記錄總量、DBR、疼痛與恢復為依據。
- 不替使用者補答案，不把不清楚的數字當成已知資料。
- 不保存帳號、卡號、登入資訊或下注帳戶細節。
